// NOTE: This file was generated by the Mojo bindings generator.
#include "mojo/public/interfaces/bindings/tests/test_structs.mojom.h"

#include <math.h>
#include <ostream>

#include "mojo/public/cpp/bindings/lib/array_serialization.h"
#include "mojo/public/cpp/bindings/lib/bindings_serialization.h"
#include "mojo/public/cpp/bindings/lib/bounds_checker.h"
#include "mojo/public/cpp/bindings/lib/map_data_internal.h"
#include "mojo/public/cpp/bindings/lib/map_serialization.h"
#include "mojo/public/cpp/bindings/lib/message_builder.h"
#include "mojo/public/cpp/bindings/lib/message_validation.h"
#include "mojo/public/cpp/bindings/lib/string_serialization.h"
#include "mojo/public/cpp/bindings/lib/validate_params.h"
#include "mojo/public/cpp/bindings/lib/validation_errors.h"
#include "mojo/public/cpp/bindings/lib/validation_util.h"
#include "mojo/public/cpp/environment/logging.h"
namespace mojo {
namespace test {

// --- Interface definitions ---
class SomeInterface_SomeMethod_ForwardToCallback
    : public mojo::MessageReceiver {
 public:
  SomeInterface_SomeMethod_ForwardToCallback(
      const SomeInterface::SomeMethodCallback& callback)
      : callback_(callback) {
  }
  bool Accept(mojo::Message* message) override;
 private:
  SomeInterface::SomeMethodCallback callback_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(SomeInterface_SomeMethod_ForwardToCallback);
};
bool SomeInterface_SomeMethod_ForwardToCallback::Accept(
    mojo::Message* message) {
  internal::SomeInterface_SomeMethod_ResponseParams_Data* params =
      reinterpret_cast<internal::SomeInterface_SomeMethod_ResponseParams_Data*>(
          message->mutable_payload());

  params->DecodePointersAndHandles(message->mutable_handles());
  
  RectPairPtr p_other_pair {};
  do {
    // NOTE: The memory backing |params| may has be smaller than
    // |sizeof(*params)| if the message comes from an older version.
    if (params->other_pair.ptr) {
      p_other_pair = RectPair::New();
      Deserialize_(params->other_pair.ptr, p_other_pair.get());
    }
  } while (false);
  callback_.Run(p_other_pair.Pass());
  return true;
}

SomeInterfaceProxy::SomeInterfaceProxy(mojo::MessageReceiverWithResponder* receiver)
    : ControlMessageProxy(receiver) {
}
void SomeInterfaceProxy::SomeMethod(
    RectPairPtr in_pair, const SomeMethodCallback& callback) {
  size_t size = sizeof(internal::SomeInterface_SomeMethod_Params_Data);
  size += in_pair.is_null()
              ? 0
              : GetSerializedSize_(*in_pair);
  mojo::RequestMessageBuilder builder(
      static_cast<uint32_t>(internal::SomeInterface_Base::MessageOrdinals::SomeMethod), size);

  internal::SomeInterface_SomeMethod_Params_Data* params =
      internal::SomeInterface_SomeMethod_Params_Data::New(builder.buffer());
  {Serialize_(in_pair.get(),
               builder.buffer(),
               &params->pair.ptr);
  }
  if (!params->pair.ptr) {
    MOJO_INTERNAL_DLOG_SERIALIZATION_WARNING(mojo::internal::ValidationError::UNEXPECTED_NULL_POINTER, "null pair in SomeInterface.SomeMethod request");
  }
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  mojo::MessageReceiver* responder =
      new SomeInterface_SomeMethod_ForwardToCallback(callback);
  if (!receiver_->AcceptWithResponder(builder.message(), responder))
    delete responder;
}
// This class implements a method's response callback: it serializes the
// response args into a mojo message and passes it to the MessageReceiver it
// was created with.
class SomeInterface_SomeMethod_ProxyToResponder
    : public SomeInterface::SomeMethodCallback::Runnable {
 public:
  ~SomeInterface_SomeMethod_ProxyToResponder() override {
    // Is the Mojo application destroying the callback without running it
    // and without first closing the pipe?
    bool callback_was_dropped = responder_ && responder_->IsValid();
    // If the Callback was dropped then deleting the responder will close
    // the pipe so the calling application knows to stop waiting for a reply.
    delete responder_;
    MOJO_DCHECK(!callback_was_dropped)  << "The callback passed to "
        "SomeInterface::SomeMethod(p_pair.Pass(), callback) "
        "was never run.";
  }

  SomeInterface_SomeMethod_ProxyToResponder(
      uint64_t request_id,
      mojo::MessageReceiverWithStatus* responder)
      : request_id_(request_id),
        responder_(responder) {
  }

  void Run(RectPairPtr in_other_pair) const override;

 private:
  uint64_t request_id_;
  mutable mojo::MessageReceiverWithStatus* responder_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(SomeInterface_SomeMethod_ProxyToResponder);
};

void SomeInterface_SomeMethod_ProxyToResponder::Run(
    RectPairPtr in_other_pair) const {
  size_t size = sizeof(internal::SomeInterface_SomeMethod_ResponseParams_Data);
  size += in_other_pair.is_null()
              ? 0
              : GetSerializedSize_(*in_other_pair);
  mojo::ResponseMessageBuilder builder(
      static_cast<uint32_t>(internal::SomeInterface_Base::MessageOrdinals::SomeMethod), size, request_id_);
  internal::SomeInterface_SomeMethod_ResponseParams_Data* params =
      internal::SomeInterface_SomeMethod_ResponseParams_Data::New(builder.buffer());
  {Serialize_(in_other_pair.get(),
               builder.buffer(),
               &params->other_pair.ptr);
  }
  if (!params->other_pair.ptr) {
    MOJO_INTERNAL_DLOG_SERIALIZATION_WARNING(mojo::internal::ValidationError::UNEXPECTED_NULL_POINTER, "null other_pair in SomeInterface.SomeMethod response");
  }
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  bool ok = responder_->Accept(builder.message());
  MOJO_ALLOW_UNUSED_LOCAL(ok);
  // TODO(darin): !ok returned here indicates a malformed message, and that may
  // be good reason to close the connection. However, we don't have a way to do
  // that from here. We should add a way.
  delete responder_;
  responder_ = nullptr;
}

SomeInterfaceStub::SomeInterfaceStub()
    : sink_(nullptr),
      control_message_handler_(SomeInterface::Version_) {
}

SomeInterfaceStub::~SomeInterfaceStub() {}

bool SomeInterfaceStub::Accept(mojo::Message* message) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.Accept(message);
  internal::SomeInterface_Base::MessageOrdinals method_ordinal =
      static_cast<internal::SomeInterface_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::SomeInterface_Base::MessageOrdinals::SomeMethod: {
      break;
    }
  }
  return false;
}

bool SomeInterfaceStub::AcceptWithResponder(
    mojo::Message* message, mojo::MessageReceiverWithStatus* responder) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.AcceptWithResponder(message, responder);
  internal::SomeInterface_Base::MessageOrdinals method_ordinal =
      static_cast<internal::SomeInterface_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::SomeInterface_Base::MessageOrdinals::SomeMethod: {
      internal::SomeInterface_SomeMethod_Params_Data* params =
          reinterpret_cast<internal::SomeInterface_SomeMethod_Params_Data*>(
              message->mutable_payload());

      params->DecodePointersAndHandles(message->mutable_handles());
      SomeInterface::SomeMethodCallback::Runnable* runnable =
          new SomeInterface_SomeMethod_ProxyToResponder(
              message->request_id(), responder);
      SomeInterface::SomeMethodCallback callback(runnable);
      
      RectPairPtr p_pair {};
      do {
        // NOTE: The memory backing |params| may has be smaller than
        // |sizeof(*params)| if the message comes from an older version.
        if (params->pair.ptr) {
          p_pair = RectPair::New();
          Deserialize_(params->pair.ptr, p_pair.get());
        }
      } while (false);
      // A null |sink_| means no implementation was bound.
      assert(sink_);
      sink_->SomeMethod(p_pair.Pass(), callback);
      return true;
    }
  }
  return false;
}

}  // namespace test
}  // namespace mojo
