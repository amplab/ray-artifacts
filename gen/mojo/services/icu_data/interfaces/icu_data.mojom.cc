// NOTE: This file was generated by the Mojo bindings generator.
#include "mojo/services/icu_data/interfaces/icu_data.mojom.h"

#include <math.h>
#include <ostream>

#include "mojo/public/cpp/bindings/lib/array_serialization.h"
#include "mojo/public/cpp/bindings/lib/bindings_serialization.h"
#include "mojo/public/cpp/bindings/lib/bounds_checker.h"
#include "mojo/public/cpp/bindings/lib/map_data_internal.h"
#include "mojo/public/cpp/bindings/lib/map_serialization.h"
#include "mojo/public/cpp/bindings/lib/message_builder.h"
#include "mojo/public/cpp/bindings/lib/message_validation.h"
#include "mojo/public/cpp/bindings/lib/string_serialization.h"
#include "mojo/public/cpp/bindings/lib/validate_params.h"
#include "mojo/public/cpp/bindings/lib/validation_errors.h"
#include "mojo/public/cpp/bindings/lib/validation_util.h"
#include "mojo/public/cpp/environment/logging.h"
namespace icu_data {

// --- Interface definitions ---
class ICUData_Map_ForwardToCallback
    : public mojo::MessageReceiver {
 public:
  ICUData_Map_ForwardToCallback(
      const ICUData::MapCallback& callback)
      : callback_(callback) {
  }
  bool Accept(mojo::Message* message) override;
 private:
  ICUData::MapCallback callback_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(ICUData_Map_ForwardToCallback);
};
bool ICUData_Map_ForwardToCallback::Accept(
    mojo::Message* message) {
  internal::ICUData_Map_ResponseParams_Data* params =
      reinterpret_cast<internal::ICUData_Map_ResponseParams_Data*>(
          message->mutable_payload());

  params->DecodePointersAndHandles(message->mutable_handles());
  
  mojo::ScopedSharedBufferHandle p_icu_data {};
  do {
    // NOTE: The memory backing |params| may has be smaller than
    // |sizeof(*params)| if the message comes from an older version.
    p_icu_data.reset(mojo::internal::FetchAndReset(&params->icu_data));
  } while (false);
  callback_.Run(p_icu_data.Pass());
  return true;
}

ICUDataProxy::ICUDataProxy(mojo::MessageReceiverWithResponder* receiver)
    : ControlMessageProxy(receiver) {
}
void ICUDataProxy::Map(
    const mojo::String& in_sha1hash, const MapCallback& callback) {
  size_t size = sizeof(internal::ICUData_Map_Params_Data);
  size += GetSerializedSize_(in_sha1hash);
  mojo::RequestMessageBuilder builder(
      static_cast<uint32_t>(internal::ICUData_Base::MessageOrdinals::Map), size);

  internal::ICUData_Map_Params_Data* params =
      internal::ICUData_Map_Params_Data::New(builder.buffer());
  SerializeString_(in_sha1hash, builder.buffer(), &params->sha1hash.ptr);
  if (!params->sha1hash.ptr) {
    MOJO_INTERNAL_DLOG_SERIALIZATION_WARNING(mojo::internal::ValidationError::UNEXPECTED_NULL_POINTER, "null sha1hash in ICUData.Map request");
  }
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  mojo::MessageReceiver* responder =
      new ICUData_Map_ForwardToCallback(callback);
  if (!receiver_->AcceptWithResponder(builder.message(), responder))
    delete responder;
}
// This class implements a method's response callback: it serializes the
// response args into a mojo message and passes it to the MessageReceiver it
// was created with.
class ICUData_Map_ProxyToResponder
    : public ICUData::MapCallback::Runnable {
 public:
  ~ICUData_Map_ProxyToResponder() override {
    // Is the Mojo application destroying the callback without running it
    // and without first closing the pipe?
    bool callback_was_dropped = responder_ && responder_->IsValid();
    // If the Callback was dropped then deleting the responder will close
    // the pipe so the calling application knows to stop waiting for a reply.
    delete responder_;
    MOJO_DCHECK(!callback_was_dropped)  << "The callback passed to "
        "ICUData::Map(p_sha1hash, callback) "
        "was never run.";
  }

  ICUData_Map_ProxyToResponder(
      uint64_t request_id,
      mojo::MessageReceiverWithStatus* responder)
      : request_id_(request_id),
        responder_(responder) {
  }

  void Run(mojo::ScopedSharedBufferHandle in_icu_data) const override;

 private:
  uint64_t request_id_;
  mutable mojo::MessageReceiverWithStatus* responder_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(ICUData_Map_ProxyToResponder);
};

void ICUData_Map_ProxyToResponder::Run(
    mojo::ScopedSharedBufferHandle in_icu_data) const {
  size_t size = sizeof(internal::ICUData_Map_ResponseParams_Data);
  mojo::ResponseMessageBuilder builder(
      static_cast<uint32_t>(internal::ICUData_Base::MessageOrdinals::Map), size, request_id_);
  internal::ICUData_Map_ResponseParams_Data* params =
      internal::ICUData_Map_ResponseParams_Data::New(builder.buffer());
  params->icu_data = in_icu_data.release();
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  bool ok = responder_->Accept(builder.message());
  MOJO_ALLOW_UNUSED_LOCAL(ok);
  // TODO(darin): !ok returned here indicates a malformed message, and that may
  // be good reason to close the connection. However, we don't have a way to do
  // that from here. We should add a way.
  delete responder_;
  responder_ = nullptr;
}

ICUDataStub::ICUDataStub()
    : sink_(nullptr),
      control_message_handler_(ICUData::Version_) {
}

ICUDataStub::~ICUDataStub() {}

bool ICUDataStub::Accept(mojo::Message* message) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.Accept(message);
  internal::ICUData_Base::MessageOrdinals method_ordinal =
      static_cast<internal::ICUData_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::ICUData_Base::MessageOrdinals::Map: {
      break;
    }
  }
  return false;
}

bool ICUDataStub::AcceptWithResponder(
    mojo::Message* message, mojo::MessageReceiverWithStatus* responder) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.AcceptWithResponder(message, responder);
  internal::ICUData_Base::MessageOrdinals method_ordinal =
      static_cast<internal::ICUData_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::ICUData_Base::MessageOrdinals::Map: {
      internal::ICUData_Map_Params_Data* params =
          reinterpret_cast<internal::ICUData_Map_Params_Data*>(
              message->mutable_payload());

      params->DecodePointersAndHandles(message->mutable_handles());
      ICUData::MapCallback::Runnable* runnable =
          new ICUData_Map_ProxyToResponder(
              message->request_id(), responder);
      ICUData::MapCallback callback(runnable);
      
      mojo::String p_sha1hash {};
      do {
        // NOTE: The memory backing |params| may has be smaller than
        // |sizeof(*params)| if the message comes from an older version.
        Deserialize_(params->sha1hash.ptr, &p_sha1hash);
      } while (false);
      // A null |sink_| means no implementation was bound.
      assert(sink_);
      sink_->Map(p_sha1hash, callback);
      return true;
    }
  }
  return false;
}

}  // namespace icu_data
