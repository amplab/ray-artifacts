// NOTE: This file was generated by the Mojo bindings generator.
#include "mojo/services/native_viewport/interfaces/native_viewport_event_dispatcher.mojom.h"

#include <math.h>
#include <ostream>

#include "mojo/public/cpp/bindings/lib/array_serialization.h"
#include "mojo/public/cpp/bindings/lib/bindings_serialization.h"
#include "mojo/public/cpp/bindings/lib/bounds_checker.h"
#include "mojo/public/cpp/bindings/lib/map_data_internal.h"
#include "mojo/public/cpp/bindings/lib/map_serialization.h"
#include "mojo/public/cpp/bindings/lib/message_builder.h"
#include "mojo/public/cpp/bindings/lib/message_validation.h"
#include "mojo/public/cpp/bindings/lib/string_serialization.h"
#include "mojo/public/cpp/bindings/lib/validate_params.h"
#include "mojo/public/cpp/bindings/lib/validation_errors.h"
#include "mojo/public/cpp/bindings/lib/validation_util.h"
#include "mojo/public/cpp/environment/logging.h"
namespace mojo {

// --- Interface definitions ---
class NativeViewportEventDispatcher_OnEvent_ForwardToCallback
    : public mojo::MessageReceiver {
 public:
  NativeViewportEventDispatcher_OnEvent_ForwardToCallback(
      const NativeViewportEventDispatcher::OnEventCallback& callback)
      : callback_(callback) {
  }
  bool Accept(mojo::Message* message) override;
 private:
  NativeViewportEventDispatcher::OnEventCallback callback_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(NativeViewportEventDispatcher_OnEvent_ForwardToCallback);
};
bool NativeViewportEventDispatcher_OnEvent_ForwardToCallback::Accept(
    mojo::Message* message) {
  internal::NativeViewportEventDispatcher_OnEvent_ResponseParams_Data* params =
      reinterpret_cast<internal::NativeViewportEventDispatcher_OnEvent_ResponseParams_Data*>(
          message->mutable_payload());

  params->DecodePointersAndHandles(message->mutable_handles());
  
  do {
    // NOTE: The memory backing |params| may has be smaller than
    // |sizeof(*params)| if the message comes from an older version.
  } while (false);
  callback_.Run();
  return true;
}

NativeViewportEventDispatcherProxy::NativeViewportEventDispatcherProxy(mojo::MessageReceiverWithResponder* receiver)
    : ControlMessageProxy(receiver) {
}
void NativeViewportEventDispatcherProxy::OnEvent(
    mojo::EventPtr in_event, const OnEventCallback& callback) {
  size_t size = sizeof(internal::NativeViewportEventDispatcher_OnEvent_Params_Data);
  size += in_event.is_null()
              ? 0
              : GetSerializedSize_(*in_event);
  mojo::RequestMessageBuilder builder(
      static_cast<uint32_t>(internal::NativeViewportEventDispatcher_Base::MessageOrdinals::OnEvent), size);

  internal::NativeViewportEventDispatcher_OnEvent_Params_Data* params =
      internal::NativeViewportEventDispatcher_OnEvent_Params_Data::New(builder.buffer());
  {Serialize_(in_event.get(),
               builder.buffer(),
               &params->event.ptr);
  }
  if (!params->event.ptr) {
    MOJO_INTERNAL_DLOG_SERIALIZATION_WARNING(mojo::internal::ValidationError::UNEXPECTED_NULL_POINTER, "null event in NativeViewportEventDispatcher.OnEvent request");
  }
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  mojo::MessageReceiver* responder =
      new NativeViewportEventDispatcher_OnEvent_ForwardToCallback(callback);
  if (!receiver_->AcceptWithResponder(builder.message(), responder))
    delete responder;
}
// This class implements a method's response callback: it serializes the
// response args into a mojo message and passes it to the MessageReceiver it
// was created with.
class NativeViewportEventDispatcher_OnEvent_ProxyToResponder
    : public NativeViewportEventDispatcher::OnEventCallback::Runnable {
 public:
  ~NativeViewportEventDispatcher_OnEvent_ProxyToResponder() override {
    // Is the Mojo application destroying the callback without running it
    // and without first closing the pipe?
    bool callback_was_dropped = responder_ && responder_->IsValid();
    // If the Callback was dropped then deleting the responder will close
    // the pipe so the calling application knows to stop waiting for a reply.
    delete responder_;
    MOJO_DCHECK(!callback_was_dropped)  << "The callback passed to "
        "NativeViewportEventDispatcher::OnEvent(p_event.Pass(), callback) "
        "was never run.";
  }

  NativeViewportEventDispatcher_OnEvent_ProxyToResponder(
      uint64_t request_id,
      mojo::MessageReceiverWithStatus* responder)
      : request_id_(request_id),
        responder_(responder) {
  }

  void Run() const override;

 private:
  uint64_t request_id_;
  mutable mojo::MessageReceiverWithStatus* responder_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(NativeViewportEventDispatcher_OnEvent_ProxyToResponder);
};

void NativeViewportEventDispatcher_OnEvent_ProxyToResponder::Run(
    ) const {
  size_t size = sizeof(internal::NativeViewportEventDispatcher_OnEvent_ResponseParams_Data);
  mojo::ResponseMessageBuilder builder(
      static_cast<uint32_t>(internal::NativeViewportEventDispatcher_Base::MessageOrdinals::OnEvent), size, request_id_);
  internal::NativeViewportEventDispatcher_OnEvent_ResponseParams_Data* params =
      internal::NativeViewportEventDispatcher_OnEvent_ResponseParams_Data::New(builder.buffer());
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  bool ok = responder_->Accept(builder.message());
  MOJO_ALLOW_UNUSED_LOCAL(ok);
  // TODO(darin): !ok returned here indicates a malformed message, and that may
  // be good reason to close the connection. However, we don't have a way to do
  // that from here. We should add a way.
  delete responder_;
  responder_ = nullptr;
}

NativeViewportEventDispatcherStub::NativeViewportEventDispatcherStub()
    : sink_(nullptr),
      control_message_handler_(NativeViewportEventDispatcher::Version_) {
}

NativeViewportEventDispatcherStub::~NativeViewportEventDispatcherStub() {}

bool NativeViewportEventDispatcherStub::Accept(mojo::Message* message) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.Accept(message);
  internal::NativeViewportEventDispatcher_Base::MessageOrdinals method_ordinal =
      static_cast<internal::NativeViewportEventDispatcher_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::NativeViewportEventDispatcher_Base::MessageOrdinals::OnEvent: {
      break;
    }
  }
  return false;
}

bool NativeViewportEventDispatcherStub::AcceptWithResponder(
    mojo::Message* message, mojo::MessageReceiverWithStatus* responder) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.AcceptWithResponder(message, responder);
  internal::NativeViewportEventDispatcher_Base::MessageOrdinals method_ordinal =
      static_cast<internal::NativeViewportEventDispatcher_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::NativeViewportEventDispatcher_Base::MessageOrdinals::OnEvent: {
      internal::NativeViewportEventDispatcher_OnEvent_Params_Data* params =
          reinterpret_cast<internal::NativeViewportEventDispatcher_OnEvent_Params_Data*>(
              message->mutable_payload());

      params->DecodePointersAndHandles(message->mutable_handles());
      NativeViewportEventDispatcher::OnEventCallback::Runnable* runnable =
          new NativeViewportEventDispatcher_OnEvent_ProxyToResponder(
              message->request_id(), responder);
      NativeViewportEventDispatcher::OnEventCallback callback(runnable);
      
      mojo::EventPtr p_event {};
      do {
        // NOTE: The memory backing |params| may has be smaller than
        // |sizeof(*params)| if the message comes from an older version.
        if (params->event.ptr) {
          p_event = mojo::Event::New();
          Deserialize_(params->event.ptr, p_event.get());
        }
      } while (false);
      // A null |sink_| means no implementation was bound.
      assert(sink_);
      sink_->OnEvent(p_event.Pass(), callback);
      return true;
    }
  }
  return false;
}

}  // namespace mojo
