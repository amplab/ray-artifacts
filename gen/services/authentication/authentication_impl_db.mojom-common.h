// NOTE: This file was generated by the Mojo bindings generator.
#ifndef SERVICES_AUTHENTICATION_AUTHENTICATION_IMPL_DB_MOJOM_COMMON_H_
#define SERVICES_AUTHENTICATION_AUTHENTICATION_IMPL_DB_MOJOM_COMMON_H_

#include <stdint.h>
#include <iosfwd>

#include "mojo/public/cpp/bindings/array.h"
#include "mojo/public/cpp/bindings/callback.h"
#include "mojo/public/cpp/bindings/interface_handle.h"
#include "mojo/public/cpp/bindings/interface_request.h"
#include "mojo/public/cpp/bindings/map.h"
#include "mojo/public/cpp/bindings/message_validator.h"
#include "mojo/public/cpp/bindings/string.h"
#include "mojo/public/cpp/bindings/struct_ptr.h"
#include "mojo/public/cpp/system/buffer.h"
#include "mojo/public/cpp/system/data_pipe.h"
#include "mojo/public/cpp/system/handle.h"
#include "mojo/public/cpp/system/message_pipe.h"
#include "services/authentication/authentication_impl_db.mojom-internal.h"
namespace authentication {

// --- Interface Forward Declarations ---

// --- Struct Forward Declarations ---
class Db;

using DbPtr = mojo::StructPtr<Db>;



// --- Union Forward Declarations ---

// --- Enums Declarations ---

// --- Constants ---

// --- Interface declarations ---
}  // namespace authentication

// --- Internal Template Specializations ---

namespace mojo {
namespace internal {
template <>
struct WrapperTraits<authentication::DbPtr, true, false> {
  using DataType = authentication::internal::Db_Data*;
};

}  // internal
}  // mojo
namespace authentication {

// --- Interface Request Validators ---

// --- Interface Response Validators ---

// --- Interface enum operators ---

// --- Unions ---
// Unions must be declared first because they can be members of structs.

// --- Inlined structs ---

// --- Non-inlined structs ---
class Db {
 public:
  using Data_ = internal::Db_Data;

  static DbPtr New();

  template <typename U>
  static DbPtr From(const U& u) {
    return mojo::TypeConverter<DbPtr, U>::Convert(u);
  }

  template <typename U>
  U To() const {
    return mojo::TypeConverter<U, Db>::Convert(*this);
  }

  Db();
  ~Db();
  
  // Returns the number of bytes it would take to serialize this struct's data.
  size_t GetSerializedSize() const;

  // Returns true on successful serialization. On failure, part of the data may
  // be serialized, until the point of failure. This API does not support
  // serializing handles. If not null, |bytes_written| is set to the number of
  // bytes written to |buf|, even if this function return false. 
  //
  // TODO(vardhan): For now, we return true for success. Should we define a
  // public error type for serialization? Should we open up
  // internal::ValidationError?
  bool Serialize(void* buf, size_t buf_size, size_t* bytes_written = nullptr);

  // Deserializes the given |buf| of size |buf_size| representing a serialized
  // version of this struct. The buffer is validated before it is deserialized.
  // Returns true on successful deserialization.
  // TODO(vardhan): Recover the validation error if there is one?
  bool Deserialize(void* buf, size_t buf_size);

  // Deserializes the given |buf| representing a serialized version of this
  // struct. The buffer is NOT validated before it is deserialized, so the user
  // must be confident of its validity and that |buf| points to enough data to
  // finish deserializing.
  void DeserializeWithoutValidation(void* buf);


  DbPtr Clone() const;
  bool Equals(const Db& other) const;

  uint32_t version;
  mojo::Map<mojo::String, mojo::String> last_selected_accounts;
};



// --- Struct serialization helpers ---
size_t GetSerializedSize_(const Db& input);
mojo::internal::ValidationError Serialize_(
    Db* input,
    mojo::internal::Buffer* buffer,
    internal::Db_Data** output);
void Deserialize_(internal::Db_Data* input,
                  Db* output);


// --- Union serialization helpers ---

// --- Request and response parameter structs for Interface methods ---
}  // namespace authentication

#endif  // SERVICES_AUTHENTICATION_AUTHENTICATION_IMPL_DB_MOJOM_COMMON_H_
