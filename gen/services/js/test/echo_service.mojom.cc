// NOTE: This file was generated by the Mojo bindings generator.
#include "services/js/test/echo_service.mojom.h"

#include <math.h>
#include <ostream>

#include "mojo/public/cpp/bindings/lib/array_serialization.h"
#include "mojo/public/cpp/bindings/lib/bindings_serialization.h"
#include "mojo/public/cpp/bindings/lib/bounds_checker.h"
#include "mojo/public/cpp/bindings/lib/map_data_internal.h"
#include "mojo/public/cpp/bindings/lib/map_serialization.h"
#include "mojo/public/cpp/bindings/lib/message_builder.h"
#include "mojo/public/cpp/bindings/lib/message_validation.h"
#include "mojo/public/cpp/bindings/lib/string_serialization.h"
#include "mojo/public/cpp/bindings/lib/validate_params.h"
#include "mojo/public/cpp/bindings/lib/validation_errors.h"
#include "mojo/public/cpp/bindings/lib/validation_util.h"
#include "mojo/public/cpp/environment/logging.h"
namespace mojo {

// --- Interface definitions ---
class EchoService_EchoString_ForwardToCallback
    : public mojo::MessageReceiver {
 public:
  EchoService_EchoString_ForwardToCallback(
      const EchoService::EchoStringCallback& callback)
      : callback_(callback) {
  }
  bool Accept(mojo::Message* message) override;
 private:
  EchoService::EchoStringCallback callback_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(EchoService_EchoString_ForwardToCallback);
};
bool EchoService_EchoString_ForwardToCallback::Accept(
    mojo::Message* message) {
  internal::EchoService_EchoString_ResponseParams_Data* params =
      reinterpret_cast<internal::EchoService_EchoString_ResponseParams_Data*>(
          message->mutable_payload());

  params->DecodePointersAndHandles(message->mutable_handles());
  
  mojo::String p_value {};
  do {
    // NOTE: The memory backing |params| may has be smaller than
    // |sizeof(*params)| if the message comes from an older version.
    Deserialize_(params->value.ptr, &p_value);
  } while (false);
  callback_.Run(p_value);
  return true;
}

EchoServiceProxy::EchoServiceProxy(mojo::MessageReceiverWithResponder* receiver)
    : ControlMessageProxy(receiver) {
}
void EchoServiceProxy::EchoString(
    const mojo::String& in_value, const EchoStringCallback& callback) {
  size_t size = sizeof(internal::EchoService_EchoString_Params_Data);
  size += GetSerializedSize_(in_value);
  mojo::RequestMessageBuilder builder(
      static_cast<uint32_t>(internal::EchoService_Base::MessageOrdinals::EchoString), size);

  internal::EchoService_EchoString_Params_Data* params =
      internal::EchoService_EchoString_Params_Data::New(builder.buffer());
  SerializeString_(in_value, builder.buffer(), &params->value.ptr);
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  mojo::MessageReceiver* responder =
      new EchoService_EchoString_ForwardToCallback(callback);
  if (!receiver_->AcceptWithResponder(builder.message(), responder))
    delete responder;
}
void EchoServiceProxy::Quit(
    ) {
  size_t size = sizeof(internal::EchoService_Quit_Params_Data);
  mojo::MessageBuilder builder(
    static_cast<uint32_t>(internal::EchoService_Base::MessageOrdinals::Quit), size);

  internal::EchoService_Quit_Params_Data* params =
      internal::EchoService_Quit_Params_Data::New(builder.buffer());
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  bool ok = receiver_->Accept(builder.message());
  // This return value may be ignored as !ok implies the Connector has
  // encountered an error, which will be visible through other means.
  MOJO_ALLOW_UNUSED_LOCAL(ok);
}
// This class implements a method's response callback: it serializes the
// response args into a mojo message and passes it to the MessageReceiver it
// was created with.
class EchoService_EchoString_ProxyToResponder
    : public EchoService::EchoStringCallback::Runnable {
 public:
  ~EchoService_EchoString_ProxyToResponder() override {
    // Is the Mojo application destroying the callback without running it
    // and without first closing the pipe?
    bool callback_was_dropped = responder_ && responder_->IsValid();
    // If the Callback was dropped then deleting the responder will close
    // the pipe so the calling application knows to stop waiting for a reply.
    delete responder_;
    MOJO_DCHECK(!callback_was_dropped)  << "The callback passed to "
        "EchoService::EchoString(p_value, callback) "
        "was never run.";
  }

  EchoService_EchoString_ProxyToResponder(
      uint64_t request_id,
      mojo::MessageReceiverWithStatus* responder)
      : request_id_(request_id),
        responder_(responder) {
  }

  void Run(const mojo::String& in_value) const override;

 private:
  uint64_t request_id_;
  mutable mojo::MessageReceiverWithStatus* responder_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(EchoService_EchoString_ProxyToResponder);
};

void EchoService_EchoString_ProxyToResponder::Run(
    const mojo::String& in_value) const {
  size_t size = sizeof(internal::EchoService_EchoString_ResponseParams_Data);
  size += GetSerializedSize_(in_value);
  mojo::ResponseMessageBuilder builder(
      static_cast<uint32_t>(internal::EchoService_Base::MessageOrdinals::EchoString), size, request_id_);
  internal::EchoService_EchoString_ResponseParams_Data* params =
      internal::EchoService_EchoString_ResponseParams_Data::New(builder.buffer());
  SerializeString_(in_value, builder.buffer(), &params->value.ptr);
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  bool ok = responder_->Accept(builder.message());
  MOJO_ALLOW_UNUSED_LOCAL(ok);
  // TODO(darin): !ok returned here indicates a malformed message, and that may
  // be good reason to close the connection. However, we don't have a way to do
  // that from here. We should add a way.
  delete responder_;
  responder_ = nullptr;
}

EchoServiceStub::EchoServiceStub()
    : sink_(nullptr),
      control_message_handler_(EchoService::Version_) {
}

EchoServiceStub::~EchoServiceStub() {}

bool EchoServiceStub::Accept(mojo::Message* message) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.Accept(message);
  internal::EchoService_Base::MessageOrdinals method_ordinal =
      static_cast<internal::EchoService_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::EchoService_Base::MessageOrdinals::EchoString: {
      break;
    }
    case internal::EchoService_Base::MessageOrdinals::Quit: {
      internal::EchoService_Quit_Params_Data* params =
          reinterpret_cast<internal::EchoService_Quit_Params_Data*>(
              message->mutable_payload());

      params->DecodePointersAndHandles(message->mutable_handles());
      
      do {
        // NOTE: The memory backing |params| may has be smaller than
        // |sizeof(*params)| if the message comes from an older version.
      } while (false);
      // A null |sink_| means no implementation was bound.
      assert(sink_);
      sink_->Quit();
      return true;
    }
  }
  return false;
}

bool EchoServiceStub::AcceptWithResponder(
    mojo::Message* message, mojo::MessageReceiverWithStatus* responder) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.AcceptWithResponder(message, responder);
  internal::EchoService_Base::MessageOrdinals method_ordinal =
      static_cast<internal::EchoService_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::EchoService_Base::MessageOrdinals::EchoString: {
      internal::EchoService_EchoString_Params_Data* params =
          reinterpret_cast<internal::EchoService_EchoString_Params_Data*>(
              message->mutable_payload());

      params->DecodePointersAndHandles(message->mutable_handles());
      EchoService::EchoStringCallback::Runnable* runnable =
          new EchoService_EchoString_ProxyToResponder(
              message->request_id(), responder);
      EchoService::EchoStringCallback callback(runnable);
      
      mojo::String p_value {};
      do {
        // NOTE: The memory backing |params| may has be smaller than
        // |sizeof(*params)| if the message comes from an older version.
        Deserialize_(params->value.ptr, &p_value);
      } while (false);
      // A null |sink_| means no implementation was bound.
      assert(sink_);
      sink_->EchoString(p_value, callback);
      return true;
    }
    case internal::EchoService_Base::MessageOrdinals::Quit: {
      break;
    }
  }
  return false;
}

}  // namespace mojo
