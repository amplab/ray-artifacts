// NOTE: This file was generated by the Mojo bindings generator.
#ifndef MOJO_SERVICES_GPU_INTERFACES_CONTEXT_PROVIDER_MOJOM_COMMON_H_
#define MOJO_SERVICES_GPU_INTERFACES_CONTEXT_PROVIDER_MOJOM_COMMON_H_

#include <stdint.h>
#include <iosfwd>

#include "mojo/public/cpp/bindings/array.h"
#include "mojo/public/cpp/bindings/callback.h"
#include "mojo/public/cpp/bindings/interface_handle.h"
#include "mojo/public/cpp/bindings/interface_request.h"
#include "mojo/public/cpp/bindings/map.h"
#include "mojo/public/cpp/bindings/message_validator.h"
#include "mojo/public/cpp/bindings/string.h"
#include "mojo/public/cpp/bindings/struct_ptr.h"
#include "mojo/public/cpp/system/buffer.h"
#include "mojo/public/cpp/system/data_pipe.h"
#include "mojo/public/cpp/system/handle.h"
#include "mojo/public/cpp/system/message_pipe.h"
#include "mojo/services/gpu/interfaces/context_provider.mojom-internal.h"
#include "mojo/services/gpu/interfaces/command_buffer.mojom-common.h"
#include "mojo/services/gpu/interfaces/viewport_parameter_listener.mojom-common.h"
namespace mojo {

// --- Interface Forward Declarations ---
class ContextProvider;
class ContextProviderRequestValidator;
class ContextProviderResponseValidator;
class ContextProvider_Synchronous;

// --- Struct Forward Declarations ---

// --- Union Forward Declarations ---

// --- Enums Declarations ---

// --- Constants ---

// --- Interface declarations ---
namespace internal {
class ContextProvider_Base {
 public:
  static const uint32_t Version_ = 0;

  using RequestValidator_ = ContextProviderRequestValidator;
  using ResponseValidator_ = ContextProviderResponseValidator;
  using Synchronous_ = ContextProvider_Synchronous;
  enum class MessageOrdinals : uint32_t {
    Create = 0,
  };


  virtual ~ContextProvider_Base() {}
};

}  // namespace internal

// Async interface declaration

class ContextProviderProxy;
class ContextProviderStub;
class ContextProvider_Synchronous;

class ContextProviderRequestValidator;
class ContextProviderResponseValidator;

class ContextProvider : public internal::ContextProvider_Base {
 public:
  virtual ~ContextProvider() override {}

  using Proxy_ = ContextProviderProxy;
  using Stub_ = ContextProviderStub;
  using CreateCallback = mojo::Callback<void(mojo::InterfaceHandle<mojo::CommandBuffer>)>;
  virtual void Create(mojo::InterfaceHandle<mojo::ViewportParameterListener> viewport_parameter_listener, const CreateCallback& callback) = 0;
};

}  // namespace mojo

// --- Internal Template Specializations ---

namespace mojo {
namespace internal {

}  // internal
}  // mojo
namespace mojo {

// --- Interface Request Validators ---
class ContextProviderRequestValidator
    : public mojo::internal::MessageValidator {
 public:
  mojo::internal::ValidationError Validate(const mojo::Message* message,
                                           std::string* err) override;
};


// --- Interface Response Validators ---
class ContextProviderResponseValidator
    : public mojo::internal::MessageValidator {
 public:
  mojo::internal::ValidationError Validate(const mojo::Message* message,
                                           std::string* err) override;
};


// --- Interface enum operators ---

// --- Unions ---
// Unions must be declared first because they can be members of structs.

// --- Inlined structs ---

// --- Non-inlined structs ---

// --- Struct serialization helpers ---

// --- Union serialization helpers ---

// --- Request and response parameter structs for Interface methods ---
      

class ContextProvider_Create_Params;

using ContextProvider_Create_ParamsPtr = mojo::StructPtr<ContextProvider_Create_Params>;


size_t GetSerializedSize_(const ContextProvider_Create_Params& input);
mojo::internal::ValidationError Serialize_(
    ContextProvider_Create_Params* input,
    mojo::internal::Buffer* buffer,
    internal::ContextProvider_Create_Params_Data** output);
void Deserialize_(internal::ContextProvider_Create_Params_Data* input,
                  ContextProvider_Create_Params* output);

class ContextProvider_Create_Params {
 public:
  using Data_ = internal::ContextProvider_Create_Params_Data;

  static ContextProvider_Create_ParamsPtr New();

  template <typename U>
  static ContextProvider_Create_ParamsPtr From(const U& u) {
    return mojo::TypeConverter<ContextProvider_Create_ParamsPtr, U>::Convert(u);
  }

  template <typename U>
  U To() const {
    return mojo::TypeConverter<U, ContextProvider_Create_Params>::Convert(*this);
  }

  ContextProvider_Create_Params();
  ~ContextProvider_Create_Params();
  
  // Returns the number of bytes it would take to serialize this struct's data.
  size_t GetSerializedSize() const;

  // Returns true on successful serialization. On failure, part of the data may
  // be serialized, until the point of failure. This API does not support
  // serializing handles. If not null, |bytes_written| is set to the number of
  // bytes written to |buf|, even if this function return false. 
  //
  // TODO(vardhan): For now, we return true for success. Should we define a
  // public error type for serialization? Should we open up
  // internal::ValidationError?
  bool Serialize(void* buf, size_t buf_size, size_t* bytes_written = nullptr);

  // Deserializes the given |buf| of size |buf_size| representing a serialized
  // version of this struct. The buffer is validated before it is deserialized.
  // Returns true on successful deserialization.
  // TODO(vardhan): Recover the validation error if there is one?
  bool Deserialize(void* buf, size_t buf_size);

  // Deserializes the given |buf| representing a serialized version of this
  // struct. The buffer is NOT validated before it is deserialized, so the user
  // must be confident of its validity and that |buf| points to enough data to
  // finish deserializing.
  void DeserializeWithoutValidation(void* buf);


  bool Equals(const ContextProvider_Create_Params& other) const;

  mojo::InterfaceHandle<mojo::ViewportParameterListener> viewport_parameter_listener;
};



class ContextProvider_Create_ResponseParams;

using ContextProvider_Create_ResponseParamsPtr = mojo::StructPtr<ContextProvider_Create_ResponseParams>;


size_t GetSerializedSize_(const ContextProvider_Create_ResponseParams& input);
mojo::internal::ValidationError Serialize_(
    ContextProvider_Create_ResponseParams* input,
    mojo::internal::Buffer* buffer,
    internal::ContextProvider_Create_ResponseParams_Data** output);
void Deserialize_(internal::ContextProvider_Create_ResponseParams_Data* input,
                  ContextProvider_Create_ResponseParams* output);

class ContextProvider_Create_ResponseParams {
 public:
  using Data_ = internal::ContextProvider_Create_ResponseParams_Data;

  static ContextProvider_Create_ResponseParamsPtr New();

  template <typename U>
  static ContextProvider_Create_ResponseParamsPtr From(const U& u) {
    return mojo::TypeConverter<ContextProvider_Create_ResponseParamsPtr, U>::Convert(u);
  }

  template <typename U>
  U To() const {
    return mojo::TypeConverter<U, ContextProvider_Create_ResponseParams>::Convert(*this);
  }

  ContextProvider_Create_ResponseParams();
  ~ContextProvider_Create_ResponseParams();
  
  // Returns the number of bytes it would take to serialize this struct's data.
  size_t GetSerializedSize() const;

  // Returns true on successful serialization. On failure, part of the data may
  // be serialized, until the point of failure. This API does not support
  // serializing handles. If not null, |bytes_written| is set to the number of
  // bytes written to |buf|, even if this function return false. 
  //
  // TODO(vardhan): For now, we return true for success. Should we define a
  // public error type for serialization? Should we open up
  // internal::ValidationError?
  bool Serialize(void* buf, size_t buf_size, size_t* bytes_written = nullptr);

  // Deserializes the given |buf| of size |buf_size| representing a serialized
  // version of this struct. The buffer is validated before it is deserialized.
  // Returns true on successful deserialization.
  // TODO(vardhan): Recover the validation error if there is one?
  bool Deserialize(void* buf, size_t buf_size);

  // Deserializes the given |buf| representing a serialized version of this
  // struct. The buffer is NOT validated before it is deserialized, so the user
  // must be confident of its validity and that |buf| points to enough data to
  // finish deserializing.
  void DeserializeWithoutValidation(void* buf);


  bool Equals(const ContextProvider_Create_ResponseParams& other) const;

  mojo::InterfaceHandle<mojo::CommandBuffer> gles2_client;
};


}  // namespace mojo

#endif  // MOJO_SERVICES_GPU_INTERFACES_CONTEXT_PROVIDER_MOJOM_COMMON_H_
