// NOTE: This file was generated by the Mojo bindings generator.
#include "mojo/public/interfaces/bindings/tests/math_calculator.mojom.h"

#include <math.h>
#include <ostream>

#include "mojo/public/cpp/bindings/lib/array_serialization.h"
#include "mojo/public/cpp/bindings/lib/bindings_serialization.h"
#include "mojo/public/cpp/bindings/lib/bounds_checker.h"
#include "mojo/public/cpp/bindings/lib/map_data_internal.h"
#include "mojo/public/cpp/bindings/lib/map_serialization.h"
#include "mojo/public/cpp/bindings/lib/message_builder.h"
#include "mojo/public/cpp/bindings/lib/message_validation.h"
#include "mojo/public/cpp/bindings/lib/string_serialization.h"
#include "mojo/public/cpp/bindings/lib/validate_params.h"
#include "mojo/public/cpp/bindings/lib/validation_errors.h"
#include "mojo/public/cpp/bindings/lib/validation_util.h"
#include "mojo/public/cpp/environment/logging.h"
namespace math {

// --- Interface definitions ---
class Calculator_Clear_ForwardToCallback
    : public mojo::MessageReceiver {
 public:
  Calculator_Clear_ForwardToCallback(
      const Calculator::ClearCallback& callback)
      : callback_(callback) {
  }
  bool Accept(mojo::Message* message) override;
 private:
  Calculator::ClearCallback callback_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(Calculator_Clear_ForwardToCallback);
};
bool Calculator_Clear_ForwardToCallback::Accept(
    mojo::Message* message) {
  internal::Calculator_Clear_ResponseParams_Data* params =
      reinterpret_cast<internal::Calculator_Clear_ResponseParams_Data*>(
          message->mutable_payload());

  params->DecodePointersAndHandles(message->mutable_handles());
  
  double p_value {};
  do {
    // NOTE: The memory backing |params| may has be smaller than
    // |sizeof(*params)| if the message comes from an older version.
    p_value = params->value;
  } while (false);
  callback_.Run(p_value);
  return true;
}
class Calculator_Add_ForwardToCallback
    : public mojo::MessageReceiver {
 public:
  Calculator_Add_ForwardToCallback(
      const Calculator::AddCallback& callback)
      : callback_(callback) {
  }
  bool Accept(mojo::Message* message) override;
 private:
  Calculator::AddCallback callback_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(Calculator_Add_ForwardToCallback);
};
bool Calculator_Add_ForwardToCallback::Accept(
    mojo::Message* message) {
  internal::Calculator_Add_ResponseParams_Data* params =
      reinterpret_cast<internal::Calculator_Add_ResponseParams_Data*>(
          message->mutable_payload());

  params->DecodePointersAndHandles(message->mutable_handles());
  
  double p_value {};
  do {
    // NOTE: The memory backing |params| may has be smaller than
    // |sizeof(*params)| if the message comes from an older version.
    p_value = params->value;
  } while (false);
  callback_.Run(p_value);
  return true;
}
class Calculator_Multiply_ForwardToCallback
    : public mojo::MessageReceiver {
 public:
  Calculator_Multiply_ForwardToCallback(
      const Calculator::MultiplyCallback& callback)
      : callback_(callback) {
  }
  bool Accept(mojo::Message* message) override;
 private:
  Calculator::MultiplyCallback callback_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(Calculator_Multiply_ForwardToCallback);
};
bool Calculator_Multiply_ForwardToCallback::Accept(
    mojo::Message* message) {
  internal::Calculator_Multiply_ResponseParams_Data* params =
      reinterpret_cast<internal::Calculator_Multiply_ResponseParams_Data*>(
          message->mutable_payload());

  params->DecodePointersAndHandles(message->mutable_handles());
  
  double p_value {};
  do {
    // NOTE: The memory backing |params| may has be smaller than
    // |sizeof(*params)| if the message comes from an older version.
    p_value = params->value;
  } while (false);
  callback_.Run(p_value);
  return true;
}

CalculatorProxy::CalculatorProxy(mojo::MessageReceiverWithResponder* receiver)
    : ControlMessageProxy(receiver) {
}
void CalculatorProxy::Clear(
    const ClearCallback& callback) {
  size_t size = sizeof(internal::Calculator_Clear_Params_Data);
  mojo::RequestMessageBuilder builder(
      static_cast<uint32_t>(internal::Calculator_Base::MessageOrdinals::Clear), size);

  internal::Calculator_Clear_Params_Data* params =
      internal::Calculator_Clear_Params_Data::New(builder.buffer());
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  mojo::MessageReceiver* responder =
      new Calculator_Clear_ForwardToCallback(callback);
  if (!receiver_->AcceptWithResponder(builder.message(), responder))
    delete responder;
}
void CalculatorProxy::Add(
    double in_value, const AddCallback& callback) {
  size_t size = sizeof(internal::Calculator_Add_Params_Data);
  mojo::RequestMessageBuilder builder(
      static_cast<uint32_t>(internal::Calculator_Base::MessageOrdinals::Add), size);

  internal::Calculator_Add_Params_Data* params =
      internal::Calculator_Add_Params_Data::New(builder.buffer());
  params->value = in_value;
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  mojo::MessageReceiver* responder =
      new Calculator_Add_ForwardToCallback(callback);
  if (!receiver_->AcceptWithResponder(builder.message(), responder))
    delete responder;
}
void CalculatorProxy::Multiply(
    double in_value, const MultiplyCallback& callback) {
  size_t size = sizeof(internal::Calculator_Multiply_Params_Data);
  mojo::RequestMessageBuilder builder(
      static_cast<uint32_t>(internal::Calculator_Base::MessageOrdinals::Multiply), size);

  internal::Calculator_Multiply_Params_Data* params =
      internal::Calculator_Multiply_Params_Data::New(builder.buffer());
  params->value = in_value;
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  mojo::MessageReceiver* responder =
      new Calculator_Multiply_ForwardToCallback(callback);
  if (!receiver_->AcceptWithResponder(builder.message(), responder))
    delete responder;
}
// This class implements a method's response callback: it serializes the
// response args into a mojo message and passes it to the MessageReceiver it
// was created with.
class Calculator_Clear_ProxyToResponder
    : public Calculator::ClearCallback::Runnable {
 public:
  ~Calculator_Clear_ProxyToResponder() override {
    // Is the Mojo application destroying the callback without running it
    // and without first closing the pipe?
    bool callback_was_dropped = responder_ && responder_->IsValid();
    // If the Callback was dropped then deleting the responder will close
    // the pipe so the calling application knows to stop waiting for a reply.
    delete responder_;
    MOJO_DCHECK(!callback_was_dropped)  << "The callback passed to "
        "Calculator::Clear(callback) "
        "was never run.";
  }

  Calculator_Clear_ProxyToResponder(
      uint64_t request_id,
      mojo::MessageReceiverWithStatus* responder)
      : request_id_(request_id),
        responder_(responder) {
  }

  void Run(double in_value) const override;

 private:
  uint64_t request_id_;
  mutable mojo::MessageReceiverWithStatus* responder_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(Calculator_Clear_ProxyToResponder);
};

void Calculator_Clear_ProxyToResponder::Run(
    double in_value) const {
  size_t size = sizeof(internal::Calculator_Clear_ResponseParams_Data);
  mojo::ResponseMessageBuilder builder(
      static_cast<uint32_t>(internal::Calculator_Base::MessageOrdinals::Clear), size, request_id_);
  internal::Calculator_Clear_ResponseParams_Data* params =
      internal::Calculator_Clear_ResponseParams_Data::New(builder.buffer());
  params->value = in_value;
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  bool ok = responder_->Accept(builder.message());
  MOJO_ALLOW_UNUSED_LOCAL(ok);
  // TODO(darin): !ok returned here indicates a malformed message, and that may
  // be good reason to close the connection. However, we don't have a way to do
  // that from here. We should add a way.
  delete responder_;
  responder_ = nullptr;
}
// This class implements a method's response callback: it serializes the
// response args into a mojo message and passes it to the MessageReceiver it
// was created with.
class Calculator_Add_ProxyToResponder
    : public Calculator::AddCallback::Runnable {
 public:
  ~Calculator_Add_ProxyToResponder() override {
    // Is the Mojo application destroying the callback without running it
    // and without first closing the pipe?
    bool callback_was_dropped = responder_ && responder_->IsValid();
    // If the Callback was dropped then deleting the responder will close
    // the pipe so the calling application knows to stop waiting for a reply.
    delete responder_;
    MOJO_DCHECK(!callback_was_dropped)  << "The callback passed to "
        "Calculator::Add(p_value, callback) "
        "was never run.";
  }

  Calculator_Add_ProxyToResponder(
      uint64_t request_id,
      mojo::MessageReceiverWithStatus* responder)
      : request_id_(request_id),
        responder_(responder) {
  }

  void Run(double in_value) const override;

 private:
  uint64_t request_id_;
  mutable mojo::MessageReceiverWithStatus* responder_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(Calculator_Add_ProxyToResponder);
};

void Calculator_Add_ProxyToResponder::Run(
    double in_value) const {
  size_t size = sizeof(internal::Calculator_Add_ResponseParams_Data);
  mojo::ResponseMessageBuilder builder(
      static_cast<uint32_t>(internal::Calculator_Base::MessageOrdinals::Add), size, request_id_);
  internal::Calculator_Add_ResponseParams_Data* params =
      internal::Calculator_Add_ResponseParams_Data::New(builder.buffer());
  params->value = in_value;
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  bool ok = responder_->Accept(builder.message());
  MOJO_ALLOW_UNUSED_LOCAL(ok);
  // TODO(darin): !ok returned here indicates a malformed message, and that may
  // be good reason to close the connection. However, we don't have a way to do
  // that from here. We should add a way.
  delete responder_;
  responder_ = nullptr;
}
// This class implements a method's response callback: it serializes the
// response args into a mojo message and passes it to the MessageReceiver it
// was created with.
class Calculator_Multiply_ProxyToResponder
    : public Calculator::MultiplyCallback::Runnable {
 public:
  ~Calculator_Multiply_ProxyToResponder() override {
    // Is the Mojo application destroying the callback without running it
    // and without first closing the pipe?
    bool callback_was_dropped = responder_ && responder_->IsValid();
    // If the Callback was dropped then deleting the responder will close
    // the pipe so the calling application knows to stop waiting for a reply.
    delete responder_;
    MOJO_DCHECK(!callback_was_dropped)  << "The callback passed to "
        "Calculator::Multiply(p_value, callback) "
        "was never run.";
  }

  Calculator_Multiply_ProxyToResponder(
      uint64_t request_id,
      mojo::MessageReceiverWithStatus* responder)
      : request_id_(request_id),
        responder_(responder) {
  }

  void Run(double in_value) const override;

 private:
  uint64_t request_id_;
  mutable mojo::MessageReceiverWithStatus* responder_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(Calculator_Multiply_ProxyToResponder);
};

void Calculator_Multiply_ProxyToResponder::Run(
    double in_value) const {
  size_t size = sizeof(internal::Calculator_Multiply_ResponseParams_Data);
  mojo::ResponseMessageBuilder builder(
      static_cast<uint32_t>(internal::Calculator_Base::MessageOrdinals::Multiply), size, request_id_);
  internal::Calculator_Multiply_ResponseParams_Data* params =
      internal::Calculator_Multiply_ResponseParams_Data::New(builder.buffer());
  params->value = in_value;
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  bool ok = responder_->Accept(builder.message());
  MOJO_ALLOW_UNUSED_LOCAL(ok);
  // TODO(darin): !ok returned here indicates a malformed message, and that may
  // be good reason to close the connection. However, we don't have a way to do
  // that from here. We should add a way.
  delete responder_;
  responder_ = nullptr;
}

CalculatorStub::CalculatorStub()
    : sink_(nullptr),
      control_message_handler_(Calculator::Version_) {
}

CalculatorStub::~CalculatorStub() {}

bool CalculatorStub::Accept(mojo::Message* message) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.Accept(message);
  internal::Calculator_Base::MessageOrdinals method_ordinal =
      static_cast<internal::Calculator_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::Calculator_Base::MessageOrdinals::Clear: {
      break;
    }
    case internal::Calculator_Base::MessageOrdinals::Add: {
      break;
    }
    case internal::Calculator_Base::MessageOrdinals::Multiply: {
      break;
    }
  }
  return false;
}

bool CalculatorStub::AcceptWithResponder(
    mojo::Message* message, mojo::MessageReceiverWithStatus* responder) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.AcceptWithResponder(message, responder);
  internal::Calculator_Base::MessageOrdinals method_ordinal =
      static_cast<internal::Calculator_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::Calculator_Base::MessageOrdinals::Clear: {
      internal::Calculator_Clear_Params_Data* params =
          reinterpret_cast<internal::Calculator_Clear_Params_Data*>(
              message->mutable_payload());

      params->DecodePointersAndHandles(message->mutable_handles());
      Calculator::ClearCallback::Runnable* runnable =
          new Calculator_Clear_ProxyToResponder(
              message->request_id(), responder);
      Calculator::ClearCallback callback(runnable);
      
      do {
        // NOTE: The memory backing |params| may has be smaller than
        // |sizeof(*params)| if the message comes from an older version.
      } while (false);
      // A null |sink_| means no implementation was bound.
      assert(sink_);
      sink_->Clear(callback);
      return true;
    }
    case internal::Calculator_Base::MessageOrdinals::Add: {
      internal::Calculator_Add_Params_Data* params =
          reinterpret_cast<internal::Calculator_Add_Params_Data*>(
              message->mutable_payload());

      params->DecodePointersAndHandles(message->mutable_handles());
      Calculator::AddCallback::Runnable* runnable =
          new Calculator_Add_ProxyToResponder(
              message->request_id(), responder);
      Calculator::AddCallback callback(runnable);
      
      double p_value {};
      do {
        // NOTE: The memory backing |params| may has be smaller than
        // |sizeof(*params)| if the message comes from an older version.
        p_value = params->value;
      } while (false);
      // A null |sink_| means no implementation was bound.
      assert(sink_);
      sink_->Add(p_value, callback);
      return true;
    }
    case internal::Calculator_Base::MessageOrdinals::Multiply: {
      internal::Calculator_Multiply_Params_Data* params =
          reinterpret_cast<internal::Calculator_Multiply_Params_Data*>(
              message->mutable_payload());

      params->DecodePointersAndHandles(message->mutable_handles());
      Calculator::MultiplyCallback::Runnable* runnable =
          new Calculator_Multiply_ProxyToResponder(
              message->request_id(), responder);
      Calculator::MultiplyCallback callback(runnable);
      
      double p_value {};
      do {
        // NOTE: The memory backing |params| may has be smaller than
        // |sizeof(*params)| if the message comes from an older version.
        p_value = params->value;
      } while (false);
      // A null |sink_| means no implementation was bound.
      assert(sink_);
      sink_->Multiply(p_value, callback);
      return true;
    }
  }
  return false;
}

}  // namespace math
