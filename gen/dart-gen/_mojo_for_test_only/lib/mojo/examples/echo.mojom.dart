// WARNING: DO NOT EDIT. This file was generated by a program.
// See $MOJO_SDK/tools/bindings/mojom_bindings_generator.py.

library echo_mojom;
import 'dart:async';
import 'package:mojo/bindings.dart' as bindings;
import 'package:mojo/core.dart' as core;
import 'package:mojo/mojo/bindings/types/service_describer.mojom.dart' as service_describer;



class ObjectInfo extends bindings.Struct {
  static const List<bindings.StructDataHeader> kVersions = const [
    const bindings.StructDataHeader(24, 0)
  ];
  int msSinceEpoch = 0;
  int numBytes = 0;

  ObjectInfo() : super(kVersions.last.size);

  ObjectInfo.init(
    int this.msSinceEpoch, 
    int this.numBytes
  ) : super(kVersions.last.size);

  static ObjectInfo deserialize(bindings.Message message) =>
      bindings.Struct.deserialize(decode, message);

  static ObjectInfo decode(bindings.Decoder decoder0) {
    if (decoder0 == null) {
      return null;
    }
    ObjectInfo result = new ObjectInfo();

    var mainDataHeader = bindings.Struct.checkVersion(decoder0, kVersions);
    if (mainDataHeader.version >= 0) {
      
      result.msSinceEpoch = decoder0.decodeInt64(8);
    }
    if (mainDataHeader.version >= 0) {
      
      result.numBytes = decoder0.decodeUint64(16);
    }
    return result;
  }

  void encode(bindings.Encoder encoder) {
    var encoder0 = encoder.getStructEncoderAtOffset(kVersions.last);
    const String structName = "ObjectInfo";
    String fieldName;
    try {
      fieldName = "msSinceEpoch";
      encoder0.encodeInt64(msSinceEpoch, 8);
      fieldName = "numBytes";
      encoder0.encodeUint64(numBytes, 16);
    } on bindings.MojoCodecError catch(e) {
      bindings.Struct.fixErrorMessage(e, fieldName, structName);
      rethrow;
    }
  }

  String toString() {
    return "ObjectInfo("
           "msSinceEpoch: $msSinceEpoch" ", "
           "numBytes: $numBytes" ")";
  }

  Map toJson() {
    Map map = new Map();
    map["msSinceEpoch"] = msSinceEpoch;
    map["numBytes"] = numBytes;
    return map;
  }
}


class _EchoEchoStringParams extends bindings.Struct {
  static const List<bindings.StructDataHeader> kVersions = const [
    const bindings.StructDataHeader(16, 0)
  ];
  int value = 0;

  _EchoEchoStringParams() : super(kVersions.last.size);

  _EchoEchoStringParams.init(
    int this.value
  ) : super(kVersions.last.size);

  static _EchoEchoStringParams deserialize(bindings.Message message) =>
      bindings.Struct.deserialize(decode, message);

  static _EchoEchoStringParams decode(bindings.Decoder decoder0) {
    if (decoder0 == null) {
      return null;
    }
    _EchoEchoStringParams result = new _EchoEchoStringParams();

    var mainDataHeader = bindings.Struct.checkVersion(decoder0, kVersions);
    if (mainDataHeader.version >= 0) {
      
      result.value = decoder0.decodeUint32(8);
    }
    return result;
  }

  void encode(bindings.Encoder encoder) {
    var encoder0 = encoder.getStructEncoderAtOffset(kVersions.last);
    const String structName = "_EchoEchoStringParams";
    String fieldName;
    try {
      fieldName = "value";
      encoder0.encodeUint32(value, 8);
    } on bindings.MojoCodecError catch(e) {
      bindings.Struct.fixErrorMessage(e, fieldName, structName);
      rethrow;
    }
  }

  String toString() {
    return "_EchoEchoStringParams("
           "value: $value" ")";
  }

  Map toJson() {
    Map map = new Map();
    map["value"] = value;
    return map;
  }
}


class EchoEchoStringResponseParams extends bindings.Struct {
  static const List<bindings.StructDataHeader> kVersions = const [
    const bindings.StructDataHeader(16, 0)
  ];
  int value = 0;

  EchoEchoStringResponseParams() : super(kVersions.last.size);

  EchoEchoStringResponseParams.init(
    int this.value
  ) : super(kVersions.last.size);

  static EchoEchoStringResponseParams deserialize(bindings.Message message) =>
      bindings.Struct.deserialize(decode, message);

  static EchoEchoStringResponseParams decode(bindings.Decoder decoder0) {
    if (decoder0 == null) {
      return null;
    }
    EchoEchoStringResponseParams result = new EchoEchoStringResponseParams();

    var mainDataHeader = bindings.Struct.checkVersion(decoder0, kVersions);
    if (mainDataHeader.version >= 0) {
      
      result.value = decoder0.decodeUint32(8);
    }
    return result;
  }

  void encode(bindings.Encoder encoder) {
    var encoder0 = encoder.getStructEncoderAtOffset(kVersions.last);
    const String structName = "EchoEchoStringResponseParams";
    String fieldName;
    try {
      fieldName = "value";
      encoder0.encodeUint32(value, 8);
    } on bindings.MojoCodecError catch(e) {
      bindings.Struct.fixErrorMessage(e, fieldName, structName);
      rethrow;
    }
  }

  String toString() {
    return "EchoEchoStringResponseParams("
           "value: $value" ")";
  }

  Map toJson() {
    Map map = new Map();
    map["value"] = value;
    return map;
  }
}


class _EchoBuildObjectParams extends bindings.Struct {
  static const List<bindings.StructDataHeader> kVersions = const [
    const bindings.StructDataHeader(24, 0)
  ];
  int objectId = 0;
  int size = 0;

  _EchoBuildObjectParams() : super(kVersions.last.size);

  _EchoBuildObjectParams.init(
    int this.objectId, 
    int this.size
  ) : super(kVersions.last.size);

  static _EchoBuildObjectParams deserialize(bindings.Message message) =>
      bindings.Struct.deserialize(decode, message);

  static _EchoBuildObjectParams decode(bindings.Decoder decoder0) {
    if (decoder0 == null) {
      return null;
    }
    _EchoBuildObjectParams result = new _EchoBuildObjectParams();

    var mainDataHeader = bindings.Struct.checkVersion(decoder0, kVersions);
    if (mainDataHeader.version >= 0) {
      
      result.objectId = decoder0.decodeInt64(8);
    }
    if (mainDataHeader.version >= 0) {
      
      result.size = decoder0.decodeUint64(16);
    }
    return result;
  }

  void encode(bindings.Encoder encoder) {
    var encoder0 = encoder.getStructEncoderAtOffset(kVersions.last);
    const String structName = "_EchoBuildObjectParams";
    String fieldName;
    try {
      fieldName = "objectId";
      encoder0.encodeInt64(objectId, 8);
      fieldName = "size";
      encoder0.encodeUint64(size, 16);
    } on bindings.MojoCodecError catch(e) {
      bindings.Struct.fixErrorMessage(e, fieldName, structName);
      rethrow;
    }
  }

  String toString() {
    return "_EchoBuildObjectParams("
           "objectId: $objectId" ", "
           "size: $size" ")";
  }

  Map toJson() {
    Map map = new Map();
    map["objectId"] = objectId;
    map["size"] = size;
    return map;
  }
}


class EchoBuildObjectResponseParams extends bindings.Struct {
  static const List<bindings.StructDataHeader> kVersions = const [
    const bindings.StructDataHeader(16, 0)
  ];
  core.MojoSharedBuffer buffer = null;

  EchoBuildObjectResponseParams() : super(kVersions.last.size);

  EchoBuildObjectResponseParams.init(
    core.MojoSharedBuffer this.buffer
  ) : super(kVersions.last.size);

  static EchoBuildObjectResponseParams deserialize(bindings.Message message) =>
      bindings.Struct.deserialize(decode, message);

  static EchoBuildObjectResponseParams decode(bindings.Decoder decoder0) {
    if (decoder0 == null) {
      return null;
    }
    EchoBuildObjectResponseParams result = new EchoBuildObjectResponseParams();

    var mainDataHeader = bindings.Struct.checkVersion(decoder0, kVersions);
    if (mainDataHeader.version >= 0) {
      
      result.buffer = decoder0.decodeSharedBufferHandle(8, false);
    }
    return result;
  }

  void encode(bindings.Encoder encoder) {
    var encoder0 = encoder.getStructEncoderAtOffset(kVersions.last);
    const String structName = "EchoBuildObjectResponseParams";
    String fieldName;
    try {
      fieldName = "buffer";
      encoder0.encodeSharedBufferHandle(buffer, 8, false);
    } on bindings.MojoCodecError catch(e) {
      bindings.Struct.fixErrorMessage(e, fieldName, structName);
      rethrow;
    }
  }

  String toString() {
    return "EchoBuildObjectResponseParams("
           "buffer: $buffer" ")";
  }

  Map toJson() {
    throw new bindings.MojoCodecError(
        'Object containing handles cannot be encoded to JSON.');
  }
}


class _EchoListObjectsParams extends bindings.Struct {
  static const List<bindings.StructDataHeader> kVersions = const [
    const bindings.StructDataHeader(8, 0)
  ];

  _EchoListObjectsParams() : super(kVersions.last.size);

  _EchoListObjectsParams.init(
  ) : super(kVersions.last.size);

  static _EchoListObjectsParams deserialize(bindings.Message message) =>
      bindings.Struct.deserialize(decode, message);

  static _EchoListObjectsParams decode(bindings.Decoder decoder0) {
    if (decoder0 == null) {
      return null;
    }
    _EchoListObjectsParams result = new _EchoListObjectsParams();

    var mainDataHeader = bindings.Struct.checkVersion(decoder0, kVersions);
    return result;
  }

  void encode(bindings.Encoder encoder) {
    encoder.getStructEncoderAtOffset(kVersions.last);
    const String structName = "_EchoListObjectsParams";
    String fieldName;
    try {
    } on bindings.MojoCodecError catch(e) {
      bindings.Struct.fixErrorMessage(e, fieldName, structName);
      rethrow;
    }
  }

  String toString() {
    return "_EchoListObjectsParams("")";
  }

  Map toJson() {
    Map map = new Map();
    return map;
  }
}


class EchoListObjectsResponseParams extends bindings.Struct {
  static const List<bindings.StructDataHeader> kVersions = const [
    const bindings.StructDataHeader(16, 0)
  ];
  List<ObjectInfo> info = null;

  EchoListObjectsResponseParams() : super(kVersions.last.size);

  EchoListObjectsResponseParams.init(
    List<ObjectInfo> this.info
  ) : super(kVersions.last.size);

  static EchoListObjectsResponseParams deserialize(bindings.Message message) =>
      bindings.Struct.deserialize(decode, message);

  static EchoListObjectsResponseParams decode(bindings.Decoder decoder0) {
    if (decoder0 == null) {
      return null;
    }
    EchoListObjectsResponseParams result = new EchoListObjectsResponseParams();

    var mainDataHeader = bindings.Struct.checkVersion(decoder0, kVersions);
    if (mainDataHeader.version >= 0) {
      
      var decoder1 = decoder0.decodePointer(8, false);
      {
        var si1 = decoder1.decodeDataHeaderForPointerArray(bindings.kUnspecifiedArrayLength);
        result.info = new List<ObjectInfo>(si1.numElements);
        for (int i1 = 0; i1 < si1.numElements; ++i1) {
          
          var decoder2 = decoder1.decodePointer(bindings.ArrayDataHeader.kHeaderSize + bindings.kPointerSize * i1, false);
          result.info[i1] = ObjectInfo.decode(decoder2);
        }
      }
    }
    return result;
  }

  void encode(bindings.Encoder encoder) {
    var encoder0 = encoder.getStructEncoderAtOffset(kVersions.last);
    const String structName = "EchoListObjectsResponseParams";
    String fieldName;
    try {
      fieldName = "info";
      if (info == null) {
        encoder0.encodeNullPointer(8, false);
      } else {
        var encoder1 = encoder0.encodePointerArray(info.length, 8, bindings.kUnspecifiedArrayLength);
        for (int i0 = 0; i0 < info.length; ++i0) {
          encoder1.encodeStruct(info[i0], bindings.ArrayDataHeader.kHeaderSize + bindings.kPointerSize * i0, false);
        }
      }
    } on bindings.MojoCodecError catch(e) {
      bindings.Struct.fixErrorMessage(e, fieldName, structName);
      rethrow;
    }
  }

  String toString() {
    return "EchoListObjectsResponseParams("
           "info: $info" ")";
  }

  Map toJson() {
    Map map = new Map();
    map["info"] = info;
    return map;
  }
}

const int _echoMethodEchoStringName = 0;
const int _echoMethodBuildObjectName = 1;
const int _echoMethodListObjectsName = 2;

class _EchoServiceDescription implements service_describer.ServiceDescription {
  void getTopLevelInterface(Function responder) {
    responder(null);
  }

  void getTypeDefinition(String typeKey, Function responder) {
    responder(null);
  }

  void getAllTypeDefinitions(Function responder) {
    responder(null);
  }
}

abstract class Echo {
  static const String serviceName = "mojo::examples::Echo";

  static service_describer.ServiceDescription _cachedServiceDescription;
  static service_describer.ServiceDescription get serviceDescription {
    if (_cachedServiceDescription == null) {
      _cachedServiceDescription = new _EchoServiceDescription();
    }
    return _cachedServiceDescription;
  }

  static EchoProxy connectToService(
      bindings.ServiceConnector s, String url, [String serviceName]) {
    EchoProxy p = new EchoProxy.unbound();
    String name = serviceName ?? Echo.serviceName;
    if ((name == null) || name.isEmpty) {
      throw new core.MojoApiError(
          "If an interface has no ServiceName, then one must be provided.");
    }
    s.connectToService(url, p, name);
    return p;
  }
  void echoString(int value,void callback(int value));
  void buildObject(int objectId,int size,void callback(core.MojoSharedBuffer buffer));
  void listObjects(void callback(List<ObjectInfo> info));
}

abstract class EchoInterface
    implements bindings.MojoInterface<Echo>,
               Echo {
  factory EchoInterface([Echo impl]) =>
      new EchoStub.unbound(impl);

  factory EchoInterface.fromEndpoint(
      core.MojoMessagePipeEndpoint endpoint,
      [Echo impl]) =>
      new EchoStub.fromEndpoint(endpoint, impl);

  factory EchoInterface.fromMock(
      Echo mock) =>
      new EchoProxy.fromMock(mock);
}

abstract class EchoInterfaceRequest
    implements bindings.MojoInterface<Echo>,
               Echo {
  factory EchoInterfaceRequest() =>
      new EchoProxy.unbound();
}

class _EchoProxyControl
    extends bindings.ProxyMessageHandler
    implements bindings.ProxyControl<Echo> {
  Echo impl;

  _EchoProxyControl.fromEndpoint(
      core.MojoMessagePipeEndpoint endpoint) : super.fromEndpoint(endpoint);

  _EchoProxyControl.fromHandle(
      core.MojoHandle handle) : super.fromHandle(handle);

  _EchoProxyControl.unbound() : super.unbound();

  String get serviceName => Echo.serviceName;

  void handleResponse(bindings.ServiceMessage message) {
    switch (message.header.type) {
      case _echoMethodEchoStringName:
        var r = EchoEchoStringResponseParams.deserialize(
            message.payload);
        if (!message.header.hasRequestId) {
          proxyError("Expected a message with a valid request Id.");
          return;
        }
        Function callback = callbackMap[message.header.requestId];
        if (callback == null) {
          proxyError(
              "Message had unknown request Id: ${message.header.requestId}");
          return;
        }
        callbackMap.remove(message.header.requestId);
        callback(r.value );
        break;
      case _echoMethodBuildObjectName:
        var r = EchoBuildObjectResponseParams.deserialize(
            message.payload);
        if (!message.header.hasRequestId) {
          proxyError("Expected a message with a valid request Id.");
          return;
        }
        Function callback = callbackMap[message.header.requestId];
        if (callback == null) {
          proxyError(
              "Message had unknown request Id: ${message.header.requestId}");
          return;
        }
        callbackMap.remove(message.header.requestId);
        callback(r.buffer );
        break;
      case _echoMethodListObjectsName:
        var r = EchoListObjectsResponseParams.deserialize(
            message.payload);
        if (!message.header.hasRequestId) {
          proxyError("Expected a message with a valid request Id.");
          return;
        }
        Function callback = callbackMap[message.header.requestId];
        if (callback == null) {
          proxyError(
              "Message had unknown request Id: ${message.header.requestId}");
          return;
        }
        callbackMap.remove(message.header.requestId);
        callback(r.info );
        break;
      default:
        proxyError("Unexpected message type: ${message.header.type}");
        close(immediate: true);
        break;
    }
  }

  @override
  String toString() {
    var superString = super.toString();
    return "_EchoProxyControl($superString)";
  }
}

class EchoProxy
    extends bindings.Proxy<Echo>
    implements Echo,
               EchoInterface,
               EchoInterfaceRequest {
  EchoProxy.fromEndpoint(
      core.MojoMessagePipeEndpoint endpoint)
      : super(new _EchoProxyControl.fromEndpoint(endpoint));

  EchoProxy.fromHandle(core.MojoHandle handle)
      : super(new _EchoProxyControl.fromHandle(handle));

  EchoProxy.unbound()
      : super(new _EchoProxyControl.unbound());

  factory EchoProxy.fromMock(Echo mock) {
    EchoProxy newMockedProxy =
        new EchoProxy.unbound();
    newMockedProxy.impl = mock;
    return newMockedProxy;
  }

  static EchoProxy newFromEndpoint(
      core.MojoMessagePipeEndpoint endpoint) {
    assert(endpoint.setDescription("For EchoProxy"));
    return new EchoProxy.fromEndpoint(endpoint);
  }


  void echoString(int value,void callback(int value)) {
    if (impl != null) {
      impl.echoString(value,callback);
      return;
    }
    var params = new _EchoEchoStringParams();
    params.value = value;
    Function zonedCallback;
    if (identical(Zone.current, Zone.ROOT)) {
      zonedCallback = callback;
    } else {
      Zone z = Zone.current;
      zonedCallback = ((int value) {
        z.bindCallback(() {
          callback(value);
        })();
      });
    }
    ctrl.sendMessageWithRequestId(
        params,
        _echoMethodEchoStringName,
        -1,
        bindings.MessageHeader.kMessageExpectsResponse,
        zonedCallback);
  }
  void buildObject(int objectId,int size,void callback(core.MojoSharedBuffer buffer)) {
    if (impl != null) {
      impl.buildObject(objectId,size,callback);
      return;
    }
    var params = new _EchoBuildObjectParams();
    params.objectId = objectId;
    params.size = size;
    Function zonedCallback;
    if (identical(Zone.current, Zone.ROOT)) {
      zonedCallback = callback;
    } else {
      Zone z = Zone.current;
      zonedCallback = ((core.MojoSharedBuffer buffer) {
        z.bindCallback(() {
          callback(buffer);
        })();
      });
    }
    ctrl.sendMessageWithRequestId(
        params,
        _echoMethodBuildObjectName,
        -1,
        bindings.MessageHeader.kMessageExpectsResponse,
        zonedCallback);
  }
  void listObjects(void callback(List<ObjectInfo> info)) {
    if (impl != null) {
      impl.listObjects(callback);
      return;
    }
    var params = new _EchoListObjectsParams();
    Function zonedCallback;
    if (identical(Zone.current, Zone.ROOT)) {
      zonedCallback = callback;
    } else {
      Zone z = Zone.current;
      zonedCallback = ((List<ObjectInfo> info) {
        z.bindCallback(() {
          callback(info);
        })();
      });
    }
    ctrl.sendMessageWithRequestId(
        params,
        _echoMethodListObjectsName,
        -1,
        bindings.MessageHeader.kMessageExpectsResponse,
        zonedCallback);
  }
}

class _EchoStubControl
    extends bindings.StubMessageHandler
    implements bindings.StubControl<Echo> {
  Echo _impl;

  _EchoStubControl.fromEndpoint(
      core.MojoMessagePipeEndpoint endpoint, [Echo impl])
      : super.fromEndpoint(endpoint, autoBegin: impl != null) {
    _impl = impl;
  }

  _EchoStubControl.fromHandle(
      core.MojoHandle handle, [Echo impl])
      : super.fromHandle(handle, autoBegin: impl != null) {
    _impl = impl;
  }

  _EchoStubControl.unbound([this._impl]) : super.unbound();

  String get serviceName => Echo.serviceName;


  Function _echoEchoStringResponseParamsResponder(
      int requestId) {
  return (int value) {
      var result = new EchoEchoStringResponseParams();
      result.value = value;
      sendResponse(buildResponseWithId(
          result,
          _echoMethodEchoStringName,
          requestId,
          bindings.MessageHeader.kMessageIsResponse));
    };
  }
  Function _echoBuildObjectResponseParamsResponder(
      int requestId) {
  return (core.MojoSharedBuffer buffer) {
      var result = new EchoBuildObjectResponseParams();
      result.buffer = buffer;
      sendResponse(buildResponseWithId(
          result,
          _echoMethodBuildObjectName,
          requestId,
          bindings.MessageHeader.kMessageIsResponse));
    };
  }
  Function _echoListObjectsResponseParamsResponder(
      int requestId) {
  return (List<ObjectInfo> info) {
      var result = new EchoListObjectsResponseParams();
      result.info = info;
      sendResponse(buildResponseWithId(
          result,
          _echoMethodListObjectsName,
          requestId,
          bindings.MessageHeader.kMessageIsResponse));
    };
  }

  void handleMessage(bindings.ServiceMessage message) {
    if (bindings.ControlMessageHandler.isControlMessage(message)) {
      bindings.ControlMessageHandler.handleMessage(
          this, 0, message);
      return;
    }
    if (_impl == null) {
      throw new core.MojoApiError("$this has no implementation set");
    }
    switch (message.header.type) {
      case _echoMethodEchoStringName:
        var params = _EchoEchoStringParams.deserialize(
            message.payload);
        _impl.echoString(params.value, _echoEchoStringResponseParamsResponder(message.header.requestId));
        break;
      case _echoMethodBuildObjectName:
        var params = _EchoBuildObjectParams.deserialize(
            message.payload);
        _impl.buildObject(params.objectId, params.size, _echoBuildObjectResponseParamsResponder(message.header.requestId));
        break;
      case _echoMethodListObjectsName:
        _impl.listObjects(_echoListObjectsResponseParamsResponder(message.header.requestId));
        break;
      default:
        throw new bindings.MojoCodecError("Unexpected message name");
        break;
    }
  }

  Echo get impl => _impl;
  set impl(Echo d) {
    if (d == null) {
      throw new core.MojoApiError("$this: Cannot set a null implementation");
    }
    if (isBound && (_impl == null)) {
      beginHandlingEvents();
    }
    _impl = d;
  }

  @override
  void bind(core.MojoMessagePipeEndpoint endpoint) {
    super.bind(endpoint);
    if (!isOpen && (_impl != null)) {
      beginHandlingEvents();
    }
  }

  @override
  String toString() {
    var superString = super.toString();
    return "_EchoStubControl($superString)";
  }

  int get version => 0;
}

class EchoStub
    extends bindings.Stub<Echo>
    implements Echo,
               EchoInterface,
               EchoInterfaceRequest {
  EchoStub.unbound([Echo impl])
      : super(new _EchoStubControl.unbound(impl));

  EchoStub.fromEndpoint(
      core.MojoMessagePipeEndpoint endpoint, [Echo impl])
      : super(new _EchoStubControl.fromEndpoint(endpoint, impl));

  EchoStub.fromHandle(
      core.MojoHandle handle, [Echo impl])
      : super(new _EchoStubControl.fromHandle(handle, impl));

  static EchoStub newFromEndpoint(
      core.MojoMessagePipeEndpoint endpoint) {
    assert(endpoint.setDescription("For EchoStub"));
    return new EchoStub.fromEndpoint(endpoint);
  }


  void echoString(int value,void callback(int value)) {
    return impl.echoString(value,callback);
  }
  void buildObject(int objectId,int size,void callback(core.MojoSharedBuffer buffer)) {
    return impl.buildObject(objectId,size,callback);
  }
  void listObjects(void callback(List<ObjectInfo> info)) {
    return impl.listObjects(callback);
  }
}



