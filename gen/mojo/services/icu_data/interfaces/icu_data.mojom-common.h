// NOTE: This file was generated by the Mojo bindings generator.
#ifndef MOJO_SERVICES_ICU_DATA_INTERFACES_ICU_DATA_MOJOM_COMMON_H_
#define MOJO_SERVICES_ICU_DATA_INTERFACES_ICU_DATA_MOJOM_COMMON_H_

#include <stdint.h>
#include <iosfwd>

#include "mojo/public/cpp/bindings/array.h"
#include "mojo/public/cpp/bindings/callback.h"
#include "mojo/public/cpp/bindings/interface_handle.h"
#include "mojo/public/cpp/bindings/interface_request.h"
#include "mojo/public/cpp/bindings/map.h"
#include "mojo/public/cpp/bindings/message_validator.h"
#include "mojo/public/cpp/bindings/string.h"
#include "mojo/public/cpp/bindings/struct_ptr.h"
#include "mojo/public/cpp/system/buffer.h"
#include "mojo/public/cpp/system/data_pipe.h"
#include "mojo/public/cpp/system/handle.h"
#include "mojo/public/cpp/system/message_pipe.h"
#include "mojo/services/icu_data/interfaces/icu_data.mojom-internal.h"
namespace icu_data {

// --- Interface Forward Declarations ---
class ICUData;
class ICUDataRequestValidator;
class ICUDataResponseValidator;
class ICUData_Synchronous;

// --- Struct Forward Declarations ---

// --- Union Forward Declarations ---

// --- Enums Declarations ---

// --- Constants ---

// --- Interface declarations ---
namespace internal {
class ICUData_Base {
 public:
  static const char Name_[];
  static const uint32_t Version_ = 0;

  using RequestValidator_ = ICUDataRequestValidator;
  using ResponseValidator_ = ICUDataResponseValidator;
  using Synchronous_ = ICUData_Synchronous;
  enum class MessageOrdinals : uint32_t {
    Map = 0,
  };


  virtual ~ICUData_Base() {}
};

}  // namespace internal

// Async interface declaration

class ICUDataProxy;
class ICUDataStub;
class ICUData_Synchronous;

class ICUDataRequestValidator;
class ICUDataResponseValidator;

class ICUData : public internal::ICUData_Base {
 public:
  virtual ~ICUData() override {}

  using Proxy_ = ICUDataProxy;
  using Stub_ = ICUDataStub;
  using MapCallback = mojo::Callback<void(mojo::ScopedSharedBufferHandle)>;
  virtual void Map(const mojo::String& sha1hash, const MapCallback& callback) = 0;
};

}  // namespace icu_data

// --- Internal Template Specializations ---

namespace mojo {
namespace internal {

}  // internal
}  // mojo
namespace icu_data {

// --- Interface Request Validators ---
class ICUDataRequestValidator
    : public mojo::internal::MessageValidator {
 public:
  mojo::internal::ValidationError Validate(const mojo::Message* message,
                                           std::string* err) override;
};


// --- Interface Response Validators ---
class ICUDataResponseValidator
    : public mojo::internal::MessageValidator {
 public:
  mojo::internal::ValidationError Validate(const mojo::Message* message,
                                           std::string* err) override;
};


// --- Interface enum operators ---

// --- Unions ---
// Unions must be declared first because they can be members of structs.

// --- Inlined structs ---

// --- Non-inlined structs ---

// --- Struct serialization helpers ---

// --- Union serialization helpers ---

// --- Request and response parameter structs for Interface methods ---
      

class ICUData_Map_Params;

using ICUData_Map_ParamsPtr = mojo::InlinedStructPtr<ICUData_Map_Params>;


size_t GetSerializedSize_(const ICUData_Map_Params& input);
mojo::internal::ValidationError Serialize_(
    ICUData_Map_Params* input,
    mojo::internal::Buffer* buffer,
    internal::ICUData_Map_Params_Data** output);
void Deserialize_(internal::ICUData_Map_Params_Data* input,
                  ICUData_Map_Params* output);

class ICUData_Map_Params {
 public:
  using Data_ = internal::ICUData_Map_Params_Data;

  static ICUData_Map_ParamsPtr New();

  template <typename U>
  static ICUData_Map_ParamsPtr From(const U& u) {
    return mojo::TypeConverter<ICUData_Map_ParamsPtr, U>::Convert(u);
  }

  template <typename U>
  U To() const {
    return mojo::TypeConverter<U, ICUData_Map_Params>::Convert(*this);
  }

  ICUData_Map_Params();
  ~ICUData_Map_Params();
  
  // Returns the number of bytes it would take to serialize this struct's data.
  size_t GetSerializedSize() const;

  // Returns true on successful serialization. On failure, part of the data may
  // be serialized, until the point of failure. This API does not support
  // serializing handles. If not null, |bytes_written| is set to the number of
  // bytes written to |buf|, even if this function return false. 
  //
  // TODO(vardhan): For now, we return true for success. Should we define a
  // public error type for serialization? Should we open up
  // internal::ValidationError?
  bool Serialize(void* buf, size_t buf_size, size_t* bytes_written = nullptr);

  // Deserializes the given |buf| of size |buf_size| representing a serialized
  // version of this struct. The buffer is validated before it is deserialized.
  // Returns true on successful deserialization.
  // TODO(vardhan): Recover the validation error if there is one?
  bool Deserialize(void* buf, size_t buf_size);

  // Deserializes the given |buf| representing a serialized version of this
  // struct. The buffer is NOT validated before it is deserialized, so the user
  // must be confident of its validity and that |buf| points to enough data to
  // finish deserializing.
  void DeserializeWithoutValidation(void* buf);


  ICUData_Map_ParamsPtr Clone() const;
  bool Equals(const ICUData_Map_Params& other) const;

  mojo::String sha1hash;
};



class ICUData_Map_ResponseParams;

using ICUData_Map_ResponseParamsPtr = mojo::StructPtr<ICUData_Map_ResponseParams>;


size_t GetSerializedSize_(const ICUData_Map_ResponseParams& input);
mojo::internal::ValidationError Serialize_(
    ICUData_Map_ResponseParams* input,
    mojo::internal::Buffer* buffer,
    internal::ICUData_Map_ResponseParams_Data** output);
void Deserialize_(internal::ICUData_Map_ResponseParams_Data* input,
                  ICUData_Map_ResponseParams* output);

class ICUData_Map_ResponseParams {
 public:
  using Data_ = internal::ICUData_Map_ResponseParams_Data;

  static ICUData_Map_ResponseParamsPtr New();

  template <typename U>
  static ICUData_Map_ResponseParamsPtr From(const U& u) {
    return mojo::TypeConverter<ICUData_Map_ResponseParamsPtr, U>::Convert(u);
  }

  template <typename U>
  U To() const {
    return mojo::TypeConverter<U, ICUData_Map_ResponseParams>::Convert(*this);
  }

  ICUData_Map_ResponseParams();
  ~ICUData_Map_ResponseParams();
  
  // Returns the number of bytes it would take to serialize this struct's data.
  size_t GetSerializedSize() const;

  // Returns true on successful serialization. On failure, part of the data may
  // be serialized, until the point of failure. This API does not support
  // serializing handles. If not null, |bytes_written| is set to the number of
  // bytes written to |buf|, even if this function return false. 
  //
  // TODO(vardhan): For now, we return true for success. Should we define a
  // public error type for serialization? Should we open up
  // internal::ValidationError?
  bool Serialize(void* buf, size_t buf_size, size_t* bytes_written = nullptr);

  // Deserializes the given |buf| of size |buf_size| representing a serialized
  // version of this struct. The buffer is validated before it is deserialized.
  // Returns true on successful deserialization.
  // TODO(vardhan): Recover the validation error if there is one?
  bool Deserialize(void* buf, size_t buf_size);

  // Deserializes the given |buf| representing a serialized version of this
  // struct. The buffer is NOT validated before it is deserialized, so the user
  // must be confident of its validity and that |buf| points to enough data to
  // finish deserializing.
  void DeserializeWithoutValidation(void* buf);


  bool Equals(const ICUData_Map_ResponseParams& other) const;

  mojo::ScopedSharedBufferHandle icu_data;
};


}  // namespace icu_data

#endif  // MOJO_SERVICES_ICU_DATA_INTERFACES_ICU_DATA_MOJOM_COMMON_H_
