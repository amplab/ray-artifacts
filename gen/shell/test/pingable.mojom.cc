// NOTE: This file was generated by the Mojo bindings generator.
#include "shell/test/pingable.mojom.h"

#include <math.h>
#include <ostream>

#include "mojo/public/cpp/bindings/lib/array_serialization.h"
#include "mojo/public/cpp/bindings/lib/bindings_serialization.h"
#include "mojo/public/cpp/bindings/lib/bounds_checker.h"
#include "mojo/public/cpp/bindings/lib/map_data_internal.h"
#include "mojo/public/cpp/bindings/lib/map_serialization.h"
#include "mojo/public/cpp/bindings/lib/message_builder.h"
#include "mojo/public/cpp/bindings/lib/message_validation.h"
#include "mojo/public/cpp/bindings/lib/string_serialization.h"
#include "mojo/public/cpp/bindings/lib/validate_params.h"
#include "mojo/public/cpp/bindings/lib/validation_errors.h"
#include "mojo/public/cpp/bindings/lib/validation_util.h"
#include "mojo/public/cpp/environment/logging.h"

// --- Interface definitions ---
class Pingable_Ping_ForwardToCallback
    : public mojo::MessageReceiver {
 public:
  Pingable_Ping_ForwardToCallback(
      const Pingable::PingCallback& callback)
      : callback_(callback) {
  }
  bool Accept(mojo::Message* message) override;
 private:
  Pingable::PingCallback callback_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(Pingable_Ping_ForwardToCallback);
};
bool Pingable_Ping_ForwardToCallback::Accept(
    mojo::Message* message) {
  internal::Pingable_Ping_ResponseParams_Data* params =
      reinterpret_cast<internal::Pingable_Ping_ResponseParams_Data*>(
          message->mutable_payload());

  params->DecodePointersAndHandles(message->mutable_handles());
  
  mojo::String p_app_url {};
  mojo::String p_connection_url {};
  mojo::String p_message {};
  do {
    // NOTE: The memory backing |params| may has be smaller than
    // |sizeof(*params)| if the message comes from an older version.
    Deserialize_(params->app_url.ptr, &p_app_url);
    Deserialize_(params->connection_url.ptr, &p_connection_url);
    Deserialize_(params->message.ptr, &p_message);
  } while (false);
  callback_.Run(p_app_url, p_connection_url, p_message);
  return true;
}

PingableProxy::PingableProxy(mojo::MessageReceiverWithResponder* receiver)
    : ControlMessageProxy(receiver) {
}
void PingableProxy::Ping(
    const mojo::String& in_message, const PingCallback& callback) {
  size_t size = sizeof(internal::Pingable_Ping_Params_Data);
  size += GetSerializedSize_(in_message);
  mojo::RequestMessageBuilder builder(
      static_cast<uint32_t>(internal::Pingable_Base::MessageOrdinals::Ping), size);

  internal::Pingable_Ping_Params_Data* params =
      internal::Pingable_Ping_Params_Data::New(builder.buffer());
  SerializeString_(in_message, builder.buffer(), &params->message.ptr);
  if (!params->message.ptr) {
    MOJO_INTERNAL_DLOG_SERIALIZATION_WARNING(mojo::internal::ValidationError::UNEXPECTED_NULL_POINTER, "null message in Pingable.Ping request");
  }
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  mojo::MessageReceiver* responder =
      new Pingable_Ping_ForwardToCallback(callback);
  if (!receiver_->AcceptWithResponder(builder.message(), responder))
    delete responder;
}
// This class implements a method's response callback: it serializes the
// response args into a mojo message and passes it to the MessageReceiver it
// was created with.
class Pingable_Ping_ProxyToResponder
    : public Pingable::PingCallback::Runnable {
 public:
  ~Pingable_Ping_ProxyToResponder() override {
    // Is the Mojo application destroying the callback without running it
    // and without first closing the pipe?
    bool callback_was_dropped = responder_ && responder_->IsValid();
    // If the Callback was dropped then deleting the responder will close
    // the pipe so the calling application knows to stop waiting for a reply.
    delete responder_;
    MOJO_DCHECK(!callback_was_dropped)  << "The callback passed to "
        "Pingable::Ping(p_message, callback) "
        "was never run.";
  }

  Pingable_Ping_ProxyToResponder(
      uint64_t request_id,
      mojo::MessageReceiverWithStatus* responder)
      : request_id_(request_id),
        responder_(responder) {
  }

  void Run(const mojo::String& in_app_url, const mojo::String& in_connection_url, const mojo::String& in_message) const override;

 private:
  uint64_t request_id_;
  mutable mojo::MessageReceiverWithStatus* responder_;
  MOJO_DISALLOW_COPY_AND_ASSIGN(Pingable_Ping_ProxyToResponder);
};

void Pingable_Ping_ProxyToResponder::Run(
    const mojo::String& in_app_url, const mojo::String& in_connection_url, const mojo::String& in_message) const {
  size_t size = sizeof(internal::Pingable_Ping_ResponseParams_Data);
  size += GetSerializedSize_(in_app_url);
  size += GetSerializedSize_(in_connection_url);
  size += GetSerializedSize_(in_message);
  mojo::ResponseMessageBuilder builder(
      static_cast<uint32_t>(internal::Pingable_Base::MessageOrdinals::Ping), size, request_id_);
  internal::Pingable_Ping_ResponseParams_Data* params =
      internal::Pingable_Ping_ResponseParams_Data::New(builder.buffer());
  SerializeString_(in_app_url, builder.buffer(), &params->app_url.ptr);
  if (!params->app_url.ptr) {
    MOJO_INTERNAL_DLOG_SERIALIZATION_WARNING(mojo::internal::ValidationError::UNEXPECTED_NULL_POINTER, "null app_url in Pingable.Ping response");
  }
  SerializeString_(in_connection_url, builder.buffer(), &params->connection_url.ptr);
  if (!params->connection_url.ptr) {
    MOJO_INTERNAL_DLOG_SERIALIZATION_WARNING(mojo::internal::ValidationError::UNEXPECTED_NULL_POINTER, "null connection_url in Pingable.Ping response");
  }
  SerializeString_(in_message, builder.buffer(), &params->message.ptr);
  if (!params->message.ptr) {
    MOJO_INTERNAL_DLOG_SERIALIZATION_WARNING(mojo::internal::ValidationError::UNEXPECTED_NULL_POINTER, "null message in Pingable.Ping response");
  }
  params->EncodePointersAndHandles(builder.message()->mutable_handles());
  bool ok = responder_->Accept(builder.message());
  MOJO_ALLOW_UNUSED_LOCAL(ok);
  // TODO(darin): !ok returned here indicates a malformed message, and that may
  // be good reason to close the connection. However, we don't have a way to do
  // that from here. We should add a way.
  delete responder_;
  responder_ = nullptr;
}

PingableStub::PingableStub()
    : sink_(nullptr),
      control_message_handler_(Pingable::Version_) {
}

PingableStub::~PingableStub() {}

bool PingableStub::Accept(mojo::Message* message) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.Accept(message);
  internal::Pingable_Base::MessageOrdinals method_ordinal =
      static_cast<internal::Pingable_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::Pingable_Base::MessageOrdinals::Ping: {
      break;
    }
  }
  return false;
}

bool PingableStub::AcceptWithResponder(
    mojo::Message* message, mojo::MessageReceiverWithStatus* responder) {
  if (mojo::internal::ControlMessageHandler::IsControlMessage(message))
    return control_message_handler_.AcceptWithResponder(message, responder);
  internal::Pingable_Base::MessageOrdinals method_ordinal =
      static_cast<internal::Pingable_Base::MessageOrdinals>(message->header()->name);
  switch (method_ordinal) {
    case internal::Pingable_Base::MessageOrdinals::Ping: {
      internal::Pingable_Ping_Params_Data* params =
          reinterpret_cast<internal::Pingable_Ping_Params_Data*>(
              message->mutable_payload());

      params->DecodePointersAndHandles(message->mutable_handles());
      Pingable::PingCallback::Runnable* runnable =
          new Pingable_Ping_ProxyToResponder(
              message->request_id(), responder);
      Pingable::PingCallback callback(runnable);
      
      mojo::String p_message {};
      do {
        // NOTE: The memory backing |params| may has be smaller than
        // |sizeof(*params)| if the message comes from an older version.
        Deserialize_(params->message.ptr, &p_message);
      } while (false);
      // A null |sink_| means no implementation was bound.
      assert(sink_);
      sink_->Ping(p_message, callback);
      return true;
    }
  }
  return false;
}

